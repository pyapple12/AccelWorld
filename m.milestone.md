# m.milestone.md —— 版本里程碑清单（格式参考 y.problems.md；命名沿用用户指定）

> 用途：记录后续版本的里程碑任务路线。格式：编号标题：内容（markdown 列表风格）。
> 本文件为规划清单，不参与运行；执行进度仍以 x.progress.md 为准。
> 版本号自 0.4.7.0 起启用四段式纯数字（见 AGENTS.md「版本体系」）。

---

## 版本 0.4.7.0 的 MileStone（工作流体系接入，2026-09-10 已发布）

1. 接入 DeepTransHub 工作流体系：文档四件套（w/x/y/z）+ m.milestone.md
2. AGENTS.md 引入工程原则与 Commit 提交规范
3. .agents/skills/ 项目级技能四件（audit-project / audit-report / progress-task / skillforge）
4. 版本号切换四段式（ver 0.46 → 0.4.7.0）

## 版本 0.4.7.1 的 MileStone（实测问题修复，任务组 T001，2026-09-10 发布）

1. 加速时间刷新频率随倍率变化（1/rate 秒节奏，P1）✅
2. 倒计时日期选择器实时反馈（快捷按钮/日历点击即勾选，P2）✅
3. 时间选择框显示不全修复（分钟可见，P3）✅
4. 运行监控体系三项落地（excepthook / qInstallMessageHandler / faulthandler，可选）→ 于 V0.4.7.2 发布

## 版本 0.4.7.2 的 MileStone（运行监控体系，任务组 T002，2026-09-10 发布）

1. 全局异常钩子：主线程/子线程未捕获异常带堆栈写入 logs/ 每日日志
2. Qt 原生警告转发：五级消息按级别接入日志体系（QSS 解析失败等可见）
3. faulthandler 原生崩溃栈落盘：logs/crash-YYYY-MM-DD.log，并入同保留期清理

## 版本 0.4.7.3 的 MileStone（短期功能项，任务组 T004，2026-09-10 发布）

1. 快捷键支持：Ctrl+S 保存 / Ctrl+Q 退出 / Ctrl+T 主题切换，键位常量入 base.json ✅
2. 倍率预设：工作/专注/睡眠一键切换（预设定义入 base.json，与滑杆/手输共用信号链）✅
3. 进度条动画：QPropertyAnimation 平滑过渡替代 setValue 跳变（时长入 base.json）✅
4. 托盘 toolTip 实时化：悬停显示当前加速时间/倍率，随 tick 更新（文本未变跳过重绘）✅
5. 附带：UI 美化方向定案归档（T003，维持现状）；已知问题 #6（GUI 退出期硬崩溃，规避方案固化）记录
6. 测试扩充：54 → 56 用例（新增倍率预设数据合法性 + 子进程隔离的配置生效断言）

## 版本 0.4.7.4 的 MileStone（第 1 轮审计修复，任务组 FIX001，2026-09-11 发布）

1. P0 配置链路加固：GBK 编码容错、损坏配置转存 .bak、JSON 原子写入（防半截文件与不可恢复丢失）✅
2. P1 功能失效修复：天气断网降级+重试回归、法定节假日中文名、默认城市启动即查询、铃声自定义→预设切换、倍率上限校验+启动脏值回退 ✅
3. P2 防御与接线：dataclass 反序列化类型校验、闹钟 created_at 防崩、倒计时恢复跨会话保留、主题持久化、子进程测试配置隔离（ACCELWORLD_CONFIG_FILE）、白色系颜色入 ui.json、天气缺字段判失败、CLI 周期同源、网络参数入配置、缓存键统一 resolve、日志跨天重开防护、保存失败上浮提示 ✅
4. P2 语义定案：一次性闹钟创建时已过时间自动顺延次日触发（替代 S8.4"仅创建当天"）✅
5. P3 批次清理 28 项：写盘去抖、对话框释放、excepthook 链式、--version 零副作用、规范与死代码清理等 ✅
6. 测试扩充：56 → 78 用例（新增 logger/monitor/gui_features 测试文件，GUI 断言子进程化沉淀）✅
7. 审计报告归档：z.plan.md 附录 A001（第 1 轮全量审计，2026-09-10）✅

## 版本 0.4.7.5 的 MileStone（第 2 轮审计修复，任务组 FIX002，2026-09-11 发布）

1. P1 回归与穿透修复：启动脏倍率回退被残留构造击穿（A001 FIX001.7 残留）、合法 JSON 非 dict 结构穿透类型校验并旁路 .bak 转存 ✅
2. P2 防御补全：NaN/Infinity 拒绝、天气响应 null/错型值判失败、读体阶段网络异常入重试白名单、白名单外保存降级、原子写 tmp 唯一化 ✅
3. P2 修复副作用治理：GUI 动画断言去时间依赖、托盘初始倍率同步持久化值、--theme light 生效、预设子进程天气打桩、GUI 测试打桩还原、启动双请求消除 ✅
4. P3 残留清理：static_config RuntimeError 承诺补全（A001 残留）、日志默认值单源化（A001 残留）、timezones 说明区（A001 遗留）、闹钟 repeat_days 口径统一、SUPPORTED_AUDIO_FORMATS 分层迁移、损坏转存节流等 ✅
5. 审计闭环：A002 报告归档（z.plan.md 附录）；_last_triggered 定案永久豁免（豁免清单①首条）✅
6. 测试扩充：78 → 90 用例（新增 tests/test_static_config.py，GUI 子进程用例扩至 12 项检查）✅

## 版本 0.4.7.6 的 MileStone（UI2.0 架构解耦，任务组 PL001，2026-09-11 发布）

1. 接口层落地：新增 interface/ 契约层（AppInterface 时钟/倍率/天气/闹钟/时区/配置/几何七域方法 + types 类型转出），UI 与后端经接口单向交互，接口无 Qt 依赖（后端+接口 pytest 可脱离 Qt 运行）✅
2. 面板纯展示化：6 面板/闹钟对话框/托盘/音频全部经接口读写，ui/ 目录后端 import 清零；闹钟管理器所有权自 UI 迁入接口 ✅
3. 展示运算出面板：新增 ui/tools/（倒计时解析与剩余拆解、进度上界换算、铃声与星期文案）✅
4. 配套改造：主题 QSS 参数化工厂、main.py 收编为装配入口（GUI 注入接口/CLI 直连后端例外）、倍率实时生效与去抖持久化拆两档（FIX001.23 语义保持）✅
5. 测试扩充：90 → 128 用例（新增接口契约 23 例 + 倒计时/时钟/闹钟展示运算 15 例；GUI 子进程桩点迁 interface 层）✅
6. 视觉零变化（仍为经典 UI，PL002 Fluent 重写待启动）✅

## 版本 0.4.7.7 的 MileStone（UI2.0 Fluent 重写，任务组 PL002，2026-09-11 发布）

1. Fluent 组件落地：接入 PyQt-Fluent-Widgets 1.11.3，六个面板与闹钟对话框换装 Fluent 组件与卡片布局，弹窗提示改非模态信息条，选择器换 Fluent 弹窗 ✅
2. 主题三态：偏好升级 跟随系统/浅色/深色，任选即持久化；默认主题定为跟随系统，注册表侦听系统深浅实时切换；非法值接口层归一化 ✅
3. 托盘 Fluent 化：菜单重建为 Fluent 风格右键弹出，通知与自绘图标保留原生（qfw 无系统能力，方案定案）✅
4. QSS 管线退役：themes.py 删除，主题色转 setThemeColor、着色改调色板与组件自带主题，ui/ 零样式表写法 ✅
5. 测试适配：三态语义重写、新增主题契约与选择器交互用例、settings 主题断言改独立证据 ✅
6. 视觉验收：多模态截图走查深/浅/auto 三态渲染与切换，界面定案 ✅

## 版本 0.4.7.8 的 MileStone（UI2.0 视觉迭代，任务组 PL003，2026-09-11 发布）

1. 多页导航：单页拆分为侧栏六导航页（时钟/倒计时/世界时钟/天气/闹钟 + 设置置底），倍率控制与时钟同页 ✅
2. 设置页：主题三段选择器（跟随系统/浅色/深色）与版本信息，主题控制自天气栏迁入（emoji 按钮退役）✅
3. Acrylic 材质：DWM 系统背板真机读回=3 实测生效，随主题深浅重铺，不支持环境静默降级；offscreen 平台短路（实测假句柄 DWM 试探毒化进程致后续窗口硬崩）✅
4. 时钟英雄区：加速时间 56px 绝对主角 + 膨胀倍率大数字，标准时间退次要行；预设按钮收身紧凑组；字号入 ui.json ✅
5. 测试架构：GUI 子进程改分阶段执行（每进程 ≤3 窗，规避多窗累积硬崩 #6 家族变体）；新增六页切换/主题双路径/选择器交互/等宽数字用例 ✅
6. 等宽数字定案：雅黑数字天然等宽（实测同宽），弃用 QFont.setFeature（PyQt6 实测毒化进程）✅

## 版本 0.5.0.0 的 MileStone（UI 2.0 落地打磨与版本收口，任务组 PL004，2026-09-11 发布）

1. 设计 tokens：页边距/间距/控件尺寸/字号收编 ui.json layout 节，面板与主窗口全部消费 tokens，散落 px 魔数清零 ✅
2. 动效审计：全部时长（进度动画/通知/去抖/定时器周期）确认单源于配置，无新增动效 ✅
3. 退出崩溃（y.problems#6）定案：矩阵实验定位崩溃于解释器退出析构阶段；main_gui 落地 os._exit 缓解（quit 前全量落盘，数据无风险），exit code 恢复真实 ✅
4. 文档同步：README 特性/操作说明/结构树、w.study 三大块架构与 GUI 分层、AGENTS 结构与版本 ✅
5. 版本策略：UI 2.0 收官定版 0.5.0.0（用户定案）✅

## 版本 0.5.0.1 的 MileStone（UI 打磨·视觉快赢与缺陷修正，任务组 PL005，2026-09-12 发布）

1. 倍率回显缺陷修复（意见书 G4）：输入框/滑杆/倍率小标签启动回显持久化倍率（interface.get_rate），控件与引擎一致 ✅
2. 常驻校验文案移除（G5）：规则提示归 InfoBar 按需弹出，输入框占位扩展 ✅
3. 类型尺度（G3 部分）：ui.json 新增 scale 六级（hero 64/倍率 30/页标题 21/城市时间 20/正文 14/说明 12），旧字号键清零 ✅
4. 构图统一（G2）：六页显式顶锚（AlignTop + 闹钟尾部收撑），页边距 24/间距 14，闹钟行按钮 36×32 ✅
5. 导航常开（PL005.05）：图标+文字展开宽 160（singleShot 延后 show 后调用，规避 qfw 状态机 pre-show 污染）✅

## 版本 0.5.1.0 的 MileStone（UI 打磨·玻璃材质系统「时之砂」，任务组 PL006，2026-09-12 发布）

1. 材质 token：ui.json 新增 field（光场渐变+双晕）/glass（tint/高光/描边/投影，深浅两套）/radius（24/18/14 三阶）+ font_family_digits（Bahnschrift）✅
2. 鎏金配色：accent 组替换鎏金 #E4B36A，新增 cool_cyan/on_accent/danger 调整，托盘图标金色跟进；闲置旧键（bg_*/text_*/disabled 等）清零 ✅
3. GlassCard 组件：ui/glass_card.py 玻璃卡（tint+顶部高光描边+柔投影）+ 窗内光场渲染器；双路径——桌面完整设计 / offscreen 安全降级（平台栅格器缺陷探针定案）✅
4. 构图与构图：六页铺玻璃（clock/settings/alarm），光场层叠于 Acrylic 之上，主题切换双层刷新 ✅
5. 可读性地板：深浅两主题最坏情形合成链对比度（深 16.1:1 / 浅 20.3:1，阈值 7:1 / 4.5:1）✅
6. Acrylic 保持定案：qfw 主题重应用会把背板重置回 2（Mica），themeChanged 挂钩 + 首拍补挂双保险保持类型 3 ✅

## 版本 0.5.2.0 的 MileStone（UI 打磨·仪表化与信息密度，任务组 PL007，2026-09-12 发布）

1. 时钟页仪表化：横条改 ProgressRing 环形表盘（语义加速小时/膨胀日不变，动画复用 progress_anim_ms 单源）；农历/月相/财神升三枚玻璃 chips（lunar_info 既有格式拆解，数据源不动）✅
2. 世界时钟矩阵：4×2 常驻城市玻璃卡（Bahnschrift 时间/星期/昼夜 ☾☀ 指示），点击卡片切换基准时区；新增 world_pins 用户配置（default_world_pins 默认全表，链路 base.json→default_factory→interface.get_world_pins）✅
3. 倒计时仪表：输入/选取降为顶部工具行，居中玻璃凸台两级字重大仪表；常用目标 chips（MM-DD 下一个 occurrence 零点，base.json quick_countdown_targets）✅
4. 天气卡：温度大数字 + 图标 + 描述 + 湿度/风速（WeatherData 结构化直填，接口零扩展）；小时级预报依赖后端扩展仍顺延 ✅
5. 动效：环形进度动画沿用 progress_anim_ms 单源；开关过渡用 qfw SwitchButton 内建（无新增动效对象）✅

## 版本 0.5.2.1 的 MileStone（「时之砂」真机观感返工，任务组 T005，2026-09-12 发布）

1. 定案入 AGENTS：视觉设计不得向测试环境妥协——offscreen/探针仅作功能与结构参考，桌面端一律按原设计实现，视觉实测以用户桌面走查为准 ✅
2. 光晕恢复真渐变：QRadialGradient 双光晕 + 线性底色（弃 24 步阶梯椭圆，色带根除；offscreen 保留实底近似仅作测试参考）✅
3. 磨砂感恢复：光场不透明度 0.94/0.92 → 0.75/0.85/0.80，DWM Acrylic 壁纸模糊重新透出 ✅
4. 圆角毛刺根除：弃 QGraphicsDropShadowEffect（中间缓冲重采样为毛刺来源），纹理按设备像素比渲染，描边回 1.0px ✅
5. 视觉验收改真机：offscreen 截图不再作为视觉验收依据；真机 NameError 缺陷（QPainterPath 漏导入）经崩溃栈监控捕获修复 ✅

## 版本 0.5.2.2 的 MileStone（「时之砂」热更新：光场抖动消色带 + 玻璃卡 specular 近似，2026-09-12 发布）

1. 光场噪点抖动：叠加 ±2% 固定种子噪点瓦片，打散 8-bit 渐变同心色带（Apple 材质同款思路）✅
2. 玻璃卡 specular 近似：方向性描边（左上受光右下收暗）+ 边缘 9px 环带提亮模拟 bezel 聚光 ✅

## 版本 0.5.2.3 的 MileStone（「时之砂」热更新：玻璃卡液态玻璃化，2026-09-12 发布）

1. 伪折射透镜层：玻璃卡内绘制正下方光场区域放大 1.07 倍（内容弯折感，向 Apple lensing 靠近）✅
2. 环境色渗染：采样卡片中心光场色增饱和后染入 tint 与 bezel 环带（同页卡片色相随位置自然变化）✅
3. 光晕源增强：glow alpha 提升（0.17→0.30/0.25），半径收敛，为"吃色"提供可采样对象 ✅
4. 修复 Painter saved states 未配平警告（透镜层裁剪块独立 save/restore）；真机抓图四页验证通过 ✅

## 版本 0.5.3.0 的 MileStone（「时之砂」玻璃渲染迭代收官，2026-09-12 发布）

1. 边缘重构：弃多层渐进亮带与双细线，回归单条 1.2px 动态 rim——颜色随边缘处光场采样变化（不再固定白）✅
2. 环境光加色透入：受光角径向光改 CompositionMode_Plus 加色混合（光只加不盖，深底无浑浊色斑）✅
3. 位置感知：角落光按卡片在窗口的实际受光量解析强度，光晕影响外的卡片不再无中生有透金 ✅
4. 磨砂还原：光场不透明度降至 0.55/0.60，DWM 模糊主导；噪点抖动保留压色带 ✅
5. 缺陷修复：非选中卡渐变缺 else 分支致左缘黑线（Qt 默认黑→白渐变）；透镜亮度 0.43 补偿卡内外对齐 ✅
6. 定案入 AGENTS：视觉设计不得向测试环境妥协；PL008 GLSL 方向预记（真折射/色散待议）✅

## 版本 0.5.3.1 的 MileStone（时钟页减法改版：标准时计卡与胶囊滑杆，2026-09-12 发布）

1. 标准时计独立成卡置顶：日期星期在上、标准时间居中大字（Bahnschrift 仪表字族），数据源 TimeInfo 不动 ✅
2. 英雄区减法：加速后一天/剩余统计、膨胀倍率大数字移除；今日膨胀进度环组改竖排（caption/环/数值）✅
3. 倍率设置减法：倍率标签、输入框、预设按钮、应用加速按钮全部移除，胶囊粗轨滑杆整行直动（拖动即生效），读数行左倍率右百分比 ✅
4. CapsuleSlider 新组件：覆写 qfw 绘制钩子实现双段胶囊轨道；底轨全宽铺满、已过段延伸至旋钮圆心外延半径——端帽半圆与旋钮同心相包；控件高度等于轨高，旋钮结构性垂直居中 ✅
5. 轨道几何参数 token 化（ui.json layout.slider_track，最终 24px；旋钮改居中绘制支持任意轨高）✅
6. 测试同步：预设按钮路径改滑杆驱动等价覆盖，倍率回显面收敛为滑杆+标签 ✅

## 版本 0.5.3.2 的 MileStone（第 3 轮审计修复：FIX003 十八项，2026-09-13 发布）

1. 正确性：倒计时常用目标支持带年份日期修复春节 chip 失效；配置超大整数与 world_pins 脏值防御补齐消除两处启动崩溃链；时间膨胀日长统一整数口径消除钟面与剩余小时矛盾 ✅
2. 防御与一致：农历串移除节气节日段修复月相 chip 反解析污染；天气手动刷新穿透缓存修复 TTL 内无效；replace_alarm 复用去重检查；created_at 非法整条拒绝防闹钟复活；损坏转存尊重写白名单 ✅
3. 清理与规范：ui.json 死键清理、闹钟对话框间距接线、interface 零调用契约方法与后端死代码批次删除、import 规范批次、说明区与版本文档同步 ✅
4. 方法沉淀：自绘圆弧留边定案入 AGENTS（TRACK_PAD 模式）；调优全过程复盘入 t.hint.md ✅

## 后续版本的 MileStone（暂不定版本号）

1. 已知问题排查：GUI 进程退出期硬崩溃（y.problems#6，P3，不阻塞功能）
2. 中大型项候选（需 OpenSpec 提案流程）：多语言界面、PyInstaller 打包发布、自定义城市
3. 1.0 大版本候选：UI 2.0（Fluent Widgets 重写 UI 层，呼应主题商店 F03c03）；长期愿景（数据统计可视化、云端配置同步等）随 1.0 规划评估
