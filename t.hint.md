## HINT001.滑杆几何调优全过程复盘（2026-09-12）

「时之砂」减法轮为时钟页自制胶囊粗轨滑杆（CapsuleSlider，覆写 qfw 1.11.3 私有钩子
`_drawHorizonGroove` / `_adjustHandlePos`）。本文记录这次调优的试错经过、最终几何
定案与方法论沉淀，供后续自绘控件调优参考。

### 最终几何定案（V0.5.3.1 现状）

- 轨高 `slider_track = 24`（ui.json 单一来源；偶数在 125% DPI 下设备像素对齐）
- 底轨全宽铺满，四周留 `TRACK_PAD = 1` 透明边距（防圆弧贴控件边被抗锯齿裁平）
- 填充 = 左右两个直径 24 正半圆端帽组成的胶囊；右端帽圆心随旋钮（同心包住旋钮），
  零进度时两端帽重合为一个整圆垫在旋钮下（"橙色圆"，用户定案必须保留）
- 旋钮可见圆外缘 22px（= 轨高 − 2×1px 橙缝），由 `_CapsuleHandle` 绘制层居中；
  行程两端内缩由"旋钮圆心对齐两端帽圆心"导出（最左圆心 = 左端帽圆心 12,12，最右同理）
- 三层绘制顺序：玻璃卡底 → 杆子（灰轨）→ 橙色填充 → 旋钮（独立子控件，最顶层）

### 试错时间线

1. 初版轨高 10px：与 22px 旋钮比例失衡（"拉杆高度没改"）
2. 14 → 23 → 24 多轮：23 为奇数，22px 旋钮在整数坐标下永远差半像素；且 qfw
   `_adjustHandlePos` 把旋钮 y 固定为 0——控件高 30 时旋钮比轨道中心高 4px
   （"旋钮没垂直居中"的根源）→ 修复：控件高 = 轨高，y=0 即铺满，绘制层负责居中
3. 端帽不同心：轨道从 x=11 起画，端帽圆心 x=22，与旋钮行程圆心 x=11 错位半个
   身位 → 改为底轨全宽、填充延伸到旋钮圆心外延半径（"半圆包住旋钮"）
4. 零进度"橙圈漏出"误判：同心圆槽比旋钮可见圆大半像素，且与控件边缘相切的浅弧
   被量化成平边 → 误诊为缺陷删掉填充；用户纠正"我原来就需要这个橙圆" → 撤销
5. 贝塞尔渐曲端帽方案：能消除折角但改动过大，用户否决 → 整体回退
6. 手调参数方案（INSET_LEFT/RIGHT、KNOB_DY/KNOB_DD）：用户试调后否决
   "不可能靠手调" → 全部删除，改为几何推导（圆心对齐端帽圆心；外缘 = 轨高 − 2×缝）
7. 最终定位真根源：**自绘圆弧与控件裁剪边界相切处，抗锯齿把圆弧量化成平边**
   （"左/上不是圆弧"的真身）→ `TRACK_PAD = 1` 四周透明边距，圆弧完整渲染 ✓

### 方法论沉淀

- 视觉问题先分类：几何错误（坐标/圆心错位）改坐标；渲染量化（相切/半像素）改
  结构（留边距）。用参数掩盖量化问题治标不治本
- 像素取证先行：offscreen QImage 逐像素分类图 + `path.contains()` 双向验证；
  断言样本要避开抗锯齿混色像素（假阴/假阳性都出现过）
- 第三方私有钩子覆写前必读源码：确认信号连接（pressed/released 需重接）、
  属性转发（setThemeColor 经 self.handle）与坐标约定（grooveLength、y=0）
- 观感验收必须真机（T005 定案）：offscreen 只能做几何断言，DPR 相位与量化效应
  只有真实桌面能暴露
- 奇偶检查前置：轨高为奇数、或轨高×DPR 非整数时必然出现半像素不对称，
  优先选偶数轨高

### 关联

- AGENTS.md「结构与约定」：自绘圆弧留边定案（TRACK_PAD 模式）
- m.milestone.md：版本 0.5.3.1 条目

---

## HINT002.UI3.0 GL 玻璃渲染复盘（2026-09-13）

### 时间线与教训

- **PL008**：GL 地基落地（GlassCanvas/场景/着色器/能力探测），真机探针发现 glDrawArrays 驱动层 GPF（127），gl_enabled 默认关闭
- **PL009 首轮**：六页铺开 + 玻璃药丸导航。功能探针全绿但**真机走查玻璃整体失效**——功能探针抓不住纯视觉缺陷
- **诊断定案三根因**：①cov 面内外判向颠倒（`-d` 写法，玻璃混合自首轮整体失效）；②Qt 合成假定预乘 alpha，直通色满亮覆盖背板（Acrylic 全灭）；③效果参数量纲坍缩（uv 探针移植主程序时改像素空间，rim/折射缩小数百倍）
- **PL009 返工**：uv 量纲回迁 + 预乘输出 + 亮度模型反转（白 veil）+ SDF 纵横比校正（药丸椭圆）+ rim 方向性；用户走查再否决 → 微黑透光背景定档（field alpha 0x23）
- **A004 审计**：GL 幽灵面（销毁不注销）/选中态 GL 缺失/makeCurrent 恒真/呼吸帧率错配等 18 项
- **PL010 收口**：性能 100 帧 1.27ms 入档；发现双 touch 定义 bug（旧覆盖新，FIX004.7 直连从未生效——帧数断言当时未跑）；GL 退出复测矩阵全过

### 方法论沉淀

- **渲染断言门**：GL 观感改动必须过 FBO 分区采样量化探针（阈值以修复前实测为基线）再进人工走查；功能探针（装配/信号断言）与视觉断言是两套独立体系
- **量纲纪律**：着色器移植/重写时第一件事核对坐标空间（uv vs 像素），效果参数随空间等比换算
- **预乘纪律**：碰 Qt 合成的帧缓冲输出，gl_FragColor 一律预乘
- **定义唯一性**：类方法重写式"修复"要先 rg 确认旧定义已删（Python 后定义覆盖前定义，静默失效）
- **真机门槛**：offscreen 功能绿 ≠ 真机观感对；GL 链路的最终裁判是真机走查（用户目测）

